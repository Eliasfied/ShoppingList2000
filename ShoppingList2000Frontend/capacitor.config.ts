import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ShoppingList2000Frontend',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
